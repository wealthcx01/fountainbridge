{
  "name": "fountainbridge-ticket-parser",
  "version": "0.1.0",
  "private": true,
  "description": "Parse docs/tickets/*.md into the bcap-contracts Ticket contract (FB-004). Isolated from the studio app (FB-005); tolerant of real ticket variation across grassmarket + fountainbridge.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "parse": "node --import tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
