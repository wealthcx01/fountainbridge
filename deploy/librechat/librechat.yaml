# LibreChat config for a venture box (FB-025). Minimal — the model surface is restricted to Claude
# (Anthropic) via `ENDPOINTS=anthropic` in `.env`, not here, to avoid coupling to LibreChat's
# fast-moving endpoint schema. Full reference: https://www.librechat.ai/docs/configuration/librechat_yaml
version: 1.2.1
cache: true
