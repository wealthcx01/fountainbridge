/** @type {import('next').NextConfig} */
const nextConfig = {
  // The studio is a read/write view onto git; no experimental features needed for the shell (FB-005).
  reactStrictMode: true,
};

export default nextConfig;
